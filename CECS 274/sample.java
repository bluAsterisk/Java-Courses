public class sample{ // class definition, needs to match the file name
  public static void main(string[] args){ // main function
    // args follow after: ./sample arg1 arg2
    int x = 3;
    while(x < 4)
    {
      system.out.println(x);
      x++;
    }
    //math order of operations first (), *, /, %, +, - last
    // server login
    // ssh 015260958@linux1.cecs.csulb.edu (only on campus)
    // ssh -p 2022 015260958@134.139.248.2 (if off campus)
  }
}
